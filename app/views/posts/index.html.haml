.wrapper
  = render "header"

  .index
    .index__frame
      .index__frame__map
      .index__frame__photo
        - @posts.each do |posts|
          = image_tag posts.image, class: "index__frame__photo__data"
